Material: light values, Textures, shader
	render(numVertecies): throw values into shader
OBJModel: v,vt,vn
	postload(): push vertex data to GPU
	render(): bind vertex data
	object: material, indexes
		postload(): push index data to GPU
		render(): bind index buffer
MD5Anim:
	render()
	render(time)
	MD5Model: ???
		render(???)
		MD5Mesh: material, ???
entity: rManager->matrecies, other values?

Shaders:
	push MVP (RenderManager)
	push vertex data (BLAHModel.object)
	push push material values (Material: Ka, map_Ka)
	push extra (Entity)
	glDraw
